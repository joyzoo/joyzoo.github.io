
.translation {
  margin-top: -20px;
  font-size: 14px;
  color: $grey-dark;
}

.post {
  background: rgba(#FFF, 0.8);
  padding: 30px;
  border-radius: 5px;
  margin-bottom: 50px;
}

.post:last-child {
  .post-eof.post-eof.post-eof {
    display: none;
  }
}
